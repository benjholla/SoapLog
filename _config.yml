#
# This file contains configuration flags to customize your site
#

# Name of your site (displayed in the header)
name: Ben's Soap Log

# Short bio or description (displayed in the header)
description: A public soaping log with a lye calculator and version control!

# URL of your avatar or profile pic (you could use your GitHub profile pic)
avatar: https://raw.githubusercontent.com/benjholla/SoapLog/master/images/logo.png

#
# Flags below are optional
#

# Includes an icon in the footer for each username you enter
footer-links:
  dribbble: 
  email: soaplog@ben-holland.com
  facebook: 
  flickr: 
  github: benjholla/SoapLog
  instagram: 
  linkedin: 
  pinterest: 
  rss: rss # just type anything here for a working RSS icon, but don't forget to set the `url` field below!
  twitter: 
  stackoverflow: # your stackoverflow profile, e.g. "users/50476/bart-kiers"

# Your disqus shortname, entering this will enable commenting on batches
disqus: soaplog

# Enter your Google Analytics web tracking code (e.g. UA-2110908-2) to activate tracking
google_analytics: 

# Your website URL (e.g. https://benjholla.github.io/SoapLog or https://benjholla.github.io or https://www.example.com)
# Used for Sitemap.xml and your RSS feed
url: https://ben-holland.com/SoapLog

# If you're hosting your site at a Project repository on GitHub pages 
# (http://yourusername.github.io/repository-name)
# and NOT your User repository (http://yourusername.github.io)
# then add in the baseurl here, like this: "/repository-name"
baseurl: "/SoapLog"

#
# !! You don't need to change any of the configuration flags below !!
#

markdown: kramdown
redcarpet:
  extensions: ["no_intra_emphasis", "fenced_code_blocks", "autolink", "tables", "strikethrough", "superscript", "with_toc_data"]
highlighter: rouge
permalink: /:title/

# The release of SoapLog that you're using
version: 1.0.0

# Set the Sass partials directory, as we're using @imports
sass:
  sass_dir: _scss
  style: :expanded # You might prefer to minify using :compressed

# Use the following plug-ins
gems:
  - jekyll-sitemap # Create a sitemap using the official Jekyll sitemap gem
  - jemoji

# Exclude these files from your production _site
exclude:
    - Gemfile
    - Gemfile.lock
    - LICENSE
    - CREDITS
    - README.md
    - .DS_Store
    - Thumbs.db
